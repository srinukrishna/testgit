
version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/testgit
 permissions:
      -object:/var/www/html/testgit
        pattern:"**"
        ownar:apache
        group:apache
        mode:755
        type:
        -file
